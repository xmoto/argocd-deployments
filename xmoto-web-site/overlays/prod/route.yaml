apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: xmoto-web-site-route-prod
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"

spec:
  parentRefs:
    - name: main
      namespace: gateway

  hostnames:
    - v2.xmoto.org

  rules:
    - backendRefs:
      - name: xmoto-web-site
        port: 80
